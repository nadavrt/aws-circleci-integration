version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:14.15
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
      - run:
          name: コードを表示かつNodeバージョンを確認
          command: |
            ls -al
            node -v